/* function sum(one: number, two?: number) {
    const result = one + two;
    if (two == 0) {
        result = one + one;
        return result;
    }

    let result = sum(2);
    console.log(result);
    
} */